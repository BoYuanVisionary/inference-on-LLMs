config_name: test

# System parameters
cuda_device_ids: [0, 1, 2, 3, 4, 5, 6, 7]
seed: 0

# Dataset and model parameters
data_path: HuggingFaceH4/MATH-500
model_name: meta-llama/Llama-3.2-1B-Instruct
reward_model_name: Qwen/Qwen2.5-Math-PRM-7B # default one
 
# Inference parameters
batch_size: 1
max_new_tokens: 2048
temperature: 0.7
top_p: 0.9

# weighted majority parameters
sampling_method: stochastic_beam_search_version4 # weighted_majority, best_of_N, majority
num_return_sequences: 1
samplingTree_temperature: 0.1
beam_width: 4
max_steps: 2
threshold: 0.9

# wandb parameters
wandb_project: efficient_reasoning

# reward method
ORM_type: product # min, last or product, geo_mean

system_prompt: |
    Solve the following math problem efficiently and clearly:
    
    - For simple problems (2 steps or fewer):
      Provide a concise solution with minimal explanation.
    
    - For complex problems (3 steps or more):
      Use this step-by-step format:
    
      ## Step 1: [Concise description]
      [Brief explanation and calculations]
    
      ## Step 2: [Concise description]
      [Brief explanation and calculations]
    
      ...
    
      Regardless of the approach, always conclude with:
    
      Therefore, the final answer is: $\boxed{answer}$. I hope it is correct.
    
      Where [answer] is just the final number or expression that solves the problem.

